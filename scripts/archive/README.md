# Archived: Manual Few-Shot Wake Word Scripts

These scripts are **deprecated**. We have pivoted to the official openWakeWord synthetic training approach.

- `record_gerty_samples.py` – Manual recording of "Gerty" (80 samples)
- `record_background.py` – Manual recording of room background
- `train_gerty_wakeword.py` – Training on manual samples (overfitted to voice detector)
- `check_gerty_samples.py` – Inspect/play recorded samples

See `docs/archive/WAKE_WORD_SYNTHETIC_TRAINING.md` for the archived synthetic training workflow. Wake word is now Picovoice "our Gurt" only.
