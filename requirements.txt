# Gerty - local AI voice assistant

# Voice
pvporcupine>=4.0.0  # Picovoice wake word (our Gurt); requires PICOVOICE_ACCESS_KEY
openwakeword>=0.6.0
vosk>=0.3.45
silero-vad>=4.0.0
faster-whisper>=1.0.0
# Moonshine STT: ~5x faster than Whisper on short commands. Optional: comment out if using faster-whisper only.
transformers[torch]>=5.3.0
piper-tts>=1.2.0
kokoro-onnx>=0.5.0  # Optional: TTS_BACKEND=kokoro for ElevenLabs-like quality
sounddevice>=0.4.6
numpy

# LLM
ollama>=0.1.0
openai>=1.55.3
httpx

# Telegram
python-telegram-bot>=21.0

# UI (qt6 backend works without system GTK packages)
pywebview[qt6]>=4.0
fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0

# Config
python-dotenv>=1.0.0

# Web search (optional, for search tool)
duckduckgo-search>=6.0.0

# Interactive browsing (optional, GERTY_BROWSE_ENABLED=1). Requires Python 3.11+
browser-use>=0.12.0
playwright>=1.40.0

# System monitoring (sys_monitor tool)
psutil>=5.9.0

# Screen capture (screen_vision tool)
mss>=9.0.0

# OpenClaw - action execution (files, browser, calendar, email). Optional: GERTY_OPENCLAW_ENABLED=1
openclaw-sdk>=2.1.0

# RAG
chromadb>=0.4.0
pypdf>=4.0.0
openpyxl>=3.1.0
python-docx>=1.0.0
